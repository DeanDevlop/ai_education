Untuk mengatasi kerentanan ini, upgrade dependensi `next` ke versi `16.1.1` atau yang lebih baru di file `package.json` Anda:


{
  "dependencies": {
    "next": "^16.1.1"
  }
}


Setelah memperbarui `package.json`, jalankan `npm install` atau `yarn install` untuk menginstal versi terbaru.